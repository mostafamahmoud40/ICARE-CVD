import { DoctorDirectoryContainer } from "./DoctorDirectoryContainer"

export const metadata = {
  title: "Doctor Directory | ICARE-CVD",
}

export default function DoctorDirectoryPage() {
  return <DoctorDirectoryContainer />
}
